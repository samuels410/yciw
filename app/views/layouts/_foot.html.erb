<%= InstFS.login_pixel(logged_in_user, session, request.host_with_port) if logged_in_user && !@files_domain %>

<script>
  INST = <%= raw(inst_env.to_json) %>;
  ENV = <%= raw StringifyIds.recursively_stringify_ids(js_env.clone).to_json %>;
</script>

<%= include_js_bundles %>
<%= include_account_js %>

<script type="text/javascript" charset="utf-8">
    console.log("load");
    //create teacher only html on course pages
    if (document.URL.indexOf('courses') > -1 && ENV.current_user_roles.indexOf('student') > -1) {

      hide(document.querySelectorAll('.target1'));

      function hide (elements) {
        elements = elements.length ? elements : [elements];
        for (var index = 0; index < elements.length; index++) {
          elements[index].style.display = 'none';
        }
      }
    }
</script>

<!-- Hotjar Tracking Code for class.yciw.net -->
<script>
  (function(h,o,t,j,a,r){
    h.hj=h.hj||function(){(h.hj.q=h.hj.q||[]).push(arguments)};
    h._hjSettings={hjid:105868,hjsv:5};
    a=o.getElementsByTagName('head')[0];
    r=o.createElement('script');r.async=1;
    r.src=t+h._hjSettings.hjid+j+h._hjSettings.hjsv;
    a.appendChild(r);
  })(window,document,'//static.hotjar.com/c/hotjar-','.js?sv=');
</script>
